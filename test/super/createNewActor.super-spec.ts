import { describe, it, expect, beforeAll, afterAll } from '@jest/globals';
import request from 'supertest';

describe(`Create New Certification Super test`, () => {
    beforeAll(async () => {
    })

    afterAll(async () => {
    });

    it(`Successfully create a new Actor`, () => {
    });
});